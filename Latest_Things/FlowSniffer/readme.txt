Move test2.c compiled file Test(ipdump(gear)) which is in the RealTest1 makefile to FlowSniffer makefile and modify the codes flowsniffer.cpp in where command string contents. 

Use the bash and system command to control the sniffer.
About Arp part, I use the libpcap as the main object for sending arp request and reply packets.
Log files are in the building_folder.
